# 万物生花 2.2 — 前端开发精简版

> 项目：抖音“视觉搜索：视觉 + 语音”赛道 Hackathon  
> 版本：2.2 最终敲定版  
> 用途：前端原型开发  
> 说明：本文件为新增精简版，原有文档保持不变

---

## 一、产品一句话

用户在抖音内容或真实场景中圈选一个画面并说一句话，AI 将这份感觉生成为花艺表达，允许用户继续编辑，并最终产出可收藏、可分享、可送礼、可拥有的结果。

---

## 二、前端开发目标

前端原型需要清晰完成以下 5 个目标：

1. 让用户知道产品有 3 种不同的生花模式  
2. 让用户完成一次内容圈选 + 语音输入  
3. 让用户看到 3 版明显不同的生花结果  
4. 让用户能对结果继续编辑  
5. 让用户看到一个明确的情绪承接结果

前端不需要证明真实算法能力，但必须把体验做得完整、可信、好看。

---

## 三、最终方案

### 3.1 产品核心

`万物生花` 不是识花工具，也不是单次生图工具。  
它是一个把内容里的感觉转成花艺表达，再把表达结果承接为情绪价值的产品。

### 3.2 三种模式

| 模式 | 适用内容 | 输出重点 |
|------|---------|---------|
| 从场景生 | 晚霞、雨景、空间、街景、静物 | 生成“氛围花”，强调画面感 |
| 从花再生 | 花束、花店成品、花艺装置 | 在原花基础上继续编辑、替换、优化 |
| 从人生生 | 人物、穿搭、关系场景、人生时刻 | 强调送礼适配、关系分寸、情绪表达 |

### 3.3 模式策略

前端采用 `自动识别 + 用户可切换` 的方式。

- 默认根据内容预选模式
- 页面顶部明确显示当前模式
- 用户可以手动切换模式

后端返回的 mock 字段建议：

```json
{
  "detected_mode": "scene",
  "confidence": 0.82
}
```

---

## 四、主链路

```text
选择内容
→ 圈选区域
→ 语音输入
→ 确认模式
→ 生成 3 版结果
→ 选择其中一版
→ 继续编辑
→ 查看情绪承接结果
→ 保存 / 分享 / 发布
```

---

## 五、页面清单

前端原型至少做 5 个页面或视图。

### 5.1 首页

#### 页面目标

让用户知道产品做什么，并理解三种模式的区别。

#### 必要模块

- 产品主标题
- 三种模式卡片
- 推荐示例素材
- 开始生花入口

#### 页面文案建议

- 标题：`把看到的感觉，生成一束花`
- 副标题：`从场景生、从花再生、从人生生`

---

### 5.2 内容选择页

#### 页面目标

让用户选择内容、圈选主体，并输入一句话。

#### 必要模块

- 图片 / 视频画布
- 圈选区域
- 当前模式提示
- 语音输入条
- 快捷示例文案

#### 快捷示例

- 从场景生：`把这场雨变成花`
- 从花再生：`把这束花做得更克制`
- 从人生生：`把她今天的感觉变成花`

---

### 5.3 生花结果页

#### 页面目标

展示 3 版结果，让用户产生“它真的懂这个画面”的感觉。

#### 必要模块

- 当前模式条
- 三版结果卡
- 每版的一句话解释
- 选择按钮
- 相似内容流入口
- 继续编辑入口

#### 三版结果要求

三版必须明显不同，不能只是轻微改色。

建议采用：

- 标题 + 小标签
- 大图优先
- 少量说明文字

示例：

- `暮色留白`
- `风里热烈`
- `雨后安静`

---

### 5.4 编辑共创页

#### 页面目标

让用户感到结果可以继续塑造，而不是一次性固定结果。

#### 必要模块

- 当前选定结果大图
- 点选花朵高亮
- 编辑工具栏
- 风格滑杆
- 语音微调条
- 快捷建议按钮

#### 必做交互

| 交互 | 前端表现 |
|------|---------|
| 点选花朵 | 高亮选中 |
| 删除花朵 | 局部刷新动效 |
| 圈选区域 | 弹出替换/重绘操作 |
| 调整滑杆 | 结果预览变化 |
| 语音微调 | 转写后更新结果 |

#### 推荐滑杆

- 热烈 ↔ 克制
- 丰盈 ↔ 留白
- 自然 ↔ 正式
- 治愈 ↔ 浪漫

---

### 5.5 情绪承接页

#### 页面目标

让用户看到结果不只是一张图，而是有下一步。

#### 必要模块

- 最终结果主卡
- 收藏卡
- 送礼卡
- 拥有卡
- 分享按钮
- 同花感内容流

#### 三类承接卡定义

| 卡片 | 内容重点 |
|------|---------|
| 收藏卡 | 保存图、标题、适合发布的短文案 |
| 送礼卡 | 适合送给谁、为什么合适、应避免什么 |
| 拥有卡 | 同感觉现货版、预算友好版、轻量桌花版 |

---

## 六、关键组件

建议前端按组件化拆分。

| 组件名 | 用途 |
|------|------|
| `ModeCard` | 模式卡片 |
| `SelectionCanvas` | 圈选画布 |
| `VoiceInputBar` | 语音输入 |
| `BloomResultCard` | 生花结果卡 |
| `SimilarContentRail` | 相似内容流 |
| `EditToolbar` | 编辑工具栏 |
| `StyleSliderGroup` | 风格滑杆组 |
| `EmotionCarryCard` | 情绪承接卡 |
| `ActionFooter` | 底部主按钮区 |

---

## 七、页面文案原则

### 7.1 要强化的表达

- 这个画面被理解了
- 这个结果可以继续改
- 这份感觉适合被送出
- 这不只是一张图

### 7.2 要避免的表达

不要把页面写成技术控制台，少用以下词：

- 算法
- 推理
- 相似度计算
- 花材替代
- 模型

### 7.3 推荐按钮文案

少用：

- 提交
- 生成
- 应用修改

多用：

- 开始生花
- 看这一版
- 再改一点
- 更适合送人
- 留住这种感觉

---

## 八、视觉要求

### 8.1 页面气质

| 页面 | 气质 |
|------|------|
| 首页 | 像内容入口，轻、吸引点击 |
| 结果页 | 像作品展示页，美感优先 |
| 编辑页 | 像轻编辑器，清晰、可操作 |
| 承接页 | 像送礼建议页，温和、可信 |

### 8.2 视觉原则

- 大图优先
- 卡片分层清晰
- 文字不过密
- 情绪感强于工具感

---

## 九、伪数据与素材

前端原型直接使用预置素材，不要求现场真实生成全部内容。

### 9.1 素材池建议

| 素材池 | 数量建议 |
|------|---------|
| 场景素材 | 8-12 张 |
| 花束素材 | 6-9 张 |
| 人物素材 | 6-9 张 |
| 结果图素材 | 每类至少 3 版 |
| 相似内容素材 | 每类 6 条以上 |

### 9.2 建议演示路径

至少准备 3 条完整路径：

1. `场景型`
晚霞 → 从场景生 → 收藏卡

2. `编辑型`
花束 → 从花再生 → 删除 / 替换 → 拥有卡

3. `关系型`
人物 → 从人生生 → 送礼卡

---

## 十、开发优先级

### P0 必做

- 首页模式入口
- 内容圈选
- 语音输入展示
- 三版结果页
- 至少一种编辑能力
- 情绪承接页

### P1 建议做

- 模式自动识别提示
- 相似内容流
- 多种快捷建议按钮
- 页面动效与过渡反馈

### P2 可不做

- 真实后端推理
- 真实局部重绘
- 复杂价格系统
- 电商真实下单

---

## 十一、前端交付标准

前端原型完成后，至少应满足以下标准：

1. 用户能在 10 秒内理解三种模式差异  
2. 用户能在 30 秒内完成一次生花  
3. 用户能清楚看出三版结果不同  
4. 用户能明显感知“结果可编辑”  
5. 用户能感受到“结果有现实承接”  

如果这 5 点成立，这版原型就足以支撑 Demo 和答辩。

---

## 十二、最终结论

这次前端开发不要把重点放在“技术说明”，而要放在四种感受上：

1. 被内容击中  
2. 被 AI 理解  
3. 可以继续共创  
4. 情绪被接住了  

前端只要把这四件事做出来，`万物生花` 的产品价值就能被评委直观看到。
