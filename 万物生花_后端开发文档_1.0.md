# 万物生花 — 后端开发文档 1.0

> 对应产品文档：`万物生花_PRD_2.3_正式版.md`  
> 用途：供编程 AI / 后端工程开发使用  
> 目标：快速搭建可支撑 Demo 的后端能力与 mock 数据服务

---

## 一、开发目标

后端需要为 `万物生花` 提供一条完整但可控的 Demo 链路：

1. 接收图片、圈选区域和语音文本
2. 识别内容模式：`scene / flower / life`
3. 抽取输入语义
4. 从本地“食花视频/花内容知识库”中检索相似参考花
5. 支持用户选择参考策略或跳过参考
6. 基于语义 + 参考花生成 3 版结果
7. 支持对选定结果做共创编辑
8. 输出最终情绪承接结果

后端当前阶段服务 Demo，不需要追求真实生产级复杂度，但需要保证：

- 接口清晰
- 数据结构稳定
- mock 数据可替换
- 链路可跑通

---

## 二、非目标

当前阶段不做以下内容：

- 真实抖音数据接入
- 真实商品下单
- 真实局部扩散重绘
- 复杂权限系统
- 用户登录体系
- 生产级监控与运维

这些内容可以保留为后续扩展点，但不进入第一版开发范围。

---

## 三、推荐技术栈

建议采用：

- Python 3.11+
- FastAPI
- Pydantic
- Uvicorn
- SQLite 或 JSON 文件作为本地知识库
- 本地文件系统存放 demo 素材与结果图

如需预留后续接模型，可将模型调用封装为 service 层。

---

## 四、系统模块

后端建议拆成以下 7 个模块。

### 4.1 输入解析模块

职责：

- 接收图片路径 / 上传文件
- 接收圈选区域
- 接收用户语音转写文本
- 统一输入结构

### 4.2 模式识别模块

职责：

- 识别当前请求属于 `scene / flower / life`
- 计算模式置信度
- 返回模式判断依据

### 4.3 语义理解模块

职责：

- 输出结构化语义标签
- 提取颜色、氛围、关系、场景、用途等信息

### 4.4 参考检索模块

职责：

- 基于语义结果检索本地花内容知识库
- 返回参考花候选
- 支持按模式过滤结果

### 4.5 融合生成模块

职责：

- 接收语义结果 + 参考策略
- 生成 3 版候选花束结果
- 输出候选图、标题、标签和解释文案

当前 Demo 阶段允许使用预置结果映射，不强制真实图像生成。

### 4.6 共创编辑模块

职责：

- 支持点击花朵查看卡片
- 支持删除花朵
- 支持局部替换
- 支持语音微调
- 返回新的结果状态

Demo 阶段允许采用“预置状态图切换”方案。

### 4.7 情绪承接模块

职责：

- 根据最终花束结果生成收藏卡、送礼卡、拥有卡
- 输出适合前端直接渲染的结构

---

## 五、目录建议

建议后端项目目录如下：

```text
backend/
  app/
    main.py
    api/
      routes_input.py
      routes_generate.py
      routes_edit.py
      routes_emotion.py
    schemas/
      input.py
      mode.py
      semantic.py
      reference.py
      bouquet.py
      emotion.py
    services/
      mode_detector.py
      semantic_parser.py
      reference_retriever.py
      bouquet_generator.py
      bouquet_editor.py
      emotion_builder.py
    repositories/
      content_repository.py
      bouquet_repository.py
    data/
      content_knowledge_base.json
      bouquets/
      assets/
    utils/
      scoring.py
      text.py
```

---

## 六、核心数据结构

### 6.1 输入结构

```json
{
  "content_id": "scene_001",
  "image_url": "/mock/scene_001.png",
  "selection_box": {
    "x": 120,
    "y": 80,
    "width": 260,
    "height": 220
  },
  "voice_text": "把这场雨变成花"
}
```

### 6.2 模式识别结果

```json
{
  "detected_mode": "scene",
  "confidence": 0.82,
  "evidence": ["rain", "wide_view", "low_human_focus"]
}
```

### 6.3 语义理解结果

```json
{
  "mode": "scene",
  "scene_tags": ["雨景", "窗边", "静谧"],
  "emotion_tags": ["克制", "治愈", "留白"],
  "color_palette": ["#B7C3D0", "#E5E8ED", "#5C6773"],
  "use_intent": "表达氛围",
  "semantic_summary": "一个偏冷色、安静、适合被转成轻留白花艺的画面"
}
```

### 6.4 参考花内容结构

```json
{
  "reference_id": "flower_ref_01",
  "title": "蓝白桌花",
  "source_type": "video",
  "cover_url": "/mock/ref_01.png",
  "mode": "scene",
  "flower_types": ["白洋桔梗", "蓝绣球", "尤加利"],
  "visual_tags": ["冷色", "轻留白", "桌花"],
  "emotion_tags": ["安静", "克制", "治愈"],
  "scene_tags": ["自用", "空间装饰"],
  "reference_options": ["color", "structure", "composition"]
}
```

### 6.5 候选花束结果

```json
{
  "result_id": "bouquet_001_v1",
  "title": "雨后安静",
  "image_url": "/mock/bouquet_001_v1.png",
  "tags": ["克制", "治愈", "适合收藏"],
  "summary": "保留了冷色和留白感，并参考了真实桌花结构",
  "reference_used": [
    {
      "reference_id": "flower_ref_01",
      "strength": "light"
    }
  ],
  "flowers": [
    {
      "flower_id": "f_01",
      "name": "白洋桔梗",
      "meaning": "温柔、克制",
      "role": "留白与主气质"
    }
  ]
}
```

### 6.6 情绪承接结果

```json
{
  "emotion_cards": {
    "save_card": {
      "title": "把这场雨留成一束花",
      "copy": "适合收藏或发布的短文案"
    },
    "gift_card": {
      "target": "适合送给刚经历阶段性变化的朋友",
      "reason": "更温和，不造成表达压力"
    },
    "own_card": {
      "options": [
        "同感觉现货版",
        "预算友好版",
        "轻量桌花版"
      ]
    }
  }
}
```

---

## 七、本地知识库设计

Demo 阶段使用本地 JSON 知识库即可。

### 7.1 知识库最小字段

每条参考内容至少包含：

- `reference_id`
- `title`
- `source_type`
- `cover_url`
- `mode`
- `flower_types`
- `visual_tags`
- `emotion_tags`
- `scene_tags`
- `reference_options`

### 7.2 知识库规模建议

至少准备：

- `scene` 模式参考内容 15 条
- `flower` 模式参考内容 15 条
- `life` 模式参考内容 15 条

总量至少 45 条，便于形成“检索感”。

### 7.3 检索逻辑建议

当前阶段不需要复杂向量检索，可先采用简单规则打分：

`总分 = 模式匹配 + 视觉标签匹配 + 情绪标签匹配 + 场景标签匹配`

例如：

- 模式匹配：40 分
- 情绪标签每命中一个：15 分
- 视觉标签每命中一个：10 分
- 场景标签每命中一个：10 分

按分数排序返回前 4-6 条。

---

## 八、接口设计

建议提供以下接口。

### 8.1 提交输入并识别模式

`POST /api/input/analyze`

请求：

```json
{
  "content_id": "scene_001",
  "image_url": "/mock/scene_001.png",
  "selection_box": {
    "x": 120,
    "y": 80,
    "width": 260,
    "height": 220
  },
  "voice_text": "把这场雨变成花"
}
```

响应：

```json
{
  "mode_result": {
    "detected_mode": "scene",
    "confidence": 0.82
  },
  "semantic_result": {
    "scene_tags": ["雨景", "安静"],
    "emotion_tags": ["克制", "治愈"],
    "semantic_summary": "一个偏冷色、安静的画面"
  }
}
```

### 8.2 检索参考花

`POST /api/reference/search`

请求：

```json
{
  "mode": "scene",
  "semantic_tags": ["克制", "治愈", "冷色", "留白"]
}
```

响应：

```json
{
  "references": [
    {
      "reference_id": "flower_ref_01",
      "title": "蓝白桌花",
      "cover_url": "/mock/ref_01.png",
      "reason": "同样具备冷色和轻留白感"
    }
  ]
}
```

### 8.3 生成三版花束

`POST /api/bouquet/generate`

请求：

```json
{
  "mode": "scene",
  "semantic_result": {
    "scene_tags": ["雨景"],
    "emotion_tags": ["克制", "治愈"]
  },
  "reference_strategy": "light",
  "selected_reference_ids": ["flower_ref_01"]
}
```

响应：

```json
{
  "results": [
    {
      "result_id": "bouquet_001_v1",
      "title": "雨后安静",
      "image_url": "/mock/bouquet_001_v1.png",
      "tags": ["克制", "治愈"],
      "summary": "参考真实桌花结构后的氛围表达"
    },
    {
      "result_id": "bouquet_001_v2",
      "title": "窗边留白",
      "image_url": "/mock/bouquet_001_v2.png",
      "tags": ["轻盈", "冷色"],
      "summary": "更偏空间感和轻留白"
    }
  ]
}
```

### 8.4 查询花朵信息

`GET /api/bouquet/{result_id}/flowers/{flower_id}`

响应：

```json
{
  "flower_id": "f_01",
  "name": "白洋桔梗",
  "type": "主花",
  "meaning": "温柔、克制",
  "role": "负责留白感与主气质"
}
```

### 8.5 编辑花束

`POST /api/bouquet/edit`

请求：

```json
{
  "result_id": "bouquet_001_v1",
  "action": "delete_flower",
  "target": {
    "flower_id": "f_02"
  },
  "instruction": "删掉中间那朵粉花"
}
```

响应：

```json
{
  "new_result_id": "bouquet_001_v1_edit_1",
  "image_url": "/mock/bouquet_001_v1_edit_1.png",
  "summary": "已移除中间粉花，整体更克制"
}
```

建议支持的 `action`：

- `delete_flower`
- `replace_region`
- `repaint_region`
- `voice_adjust`

### 8.6 获取情绪承接结果

`POST /api/emotion/build`

请求：

```json
{
  "result_id": "bouquet_001_v1_edit_1",
  "mode": "scene",
  "voice_context": "送给刚升职的朋友，别太甜"
}
```

响应：

```json
{
  "save_card": {
    "title": "把雨后的安静留成一束花",
    "copy": "适合收藏和发布的短文案"
  },
  "gift_card": {
    "target": "刚升职的朋友",
    "reason": "更温和、不过分亲密"
  },
  "own_card": {
    "options": [
      "同感觉现货版",
      "预算友好版",
      "轻量桌花版"
    ]
  }
}
```

---

## 九、服务实现建议

### 9.1 模式识别

优先级：

1. 如果输入中花束/花朵占主导，返回 `flower`
2. 如果输入中人物占主导，返回 `life`
3. 其他场景默认 `scene`

可辅以语音关键词增强判断：

- 出现“这束花、配色、删掉这朵”优先 `flower`
- 出现“她、他、朋友、生日、适合送”优先 `life`
- 出现“晚霞、房间、下雨、这个场景”优先 `scene`

### 9.2 语义理解

当前阶段可以采用：

- 基于 `content_id` 预置标签
- 结合 `voice_text` 做规则补充

例如：

- `scene_001` 默认带 `["晚霞", "温柔", "暖色"]`
- 如果语音出现“送朋友”，再加 `use_intent = gift`

### 9.3 融合生成

当前阶段不要求真实模型调用，可采用：

- 预置结果模板
- 根据 `mode + semantic tags + reference strategy` 选取不同结果图

确保三版结果明显不同。

### 9.4 编辑能力

编辑能力允许使用预置状态切换：

- 原图
- 删除某朵后的图
- 替换后的图
- 语音微调后的图

不要求当前阶段实现真实图像局部生成。

---

## 十、开发顺序

建议按以下顺序完成。

### P0

1. 输入解析接口
2. 模式识别接口
3. 语义理解接口
4. 参考检索接口
5. 三版结果生成接口
6. 情绪承接接口

### P1

1. 查询花朵信息接口
2. 编辑花束接口
3. 基于指令切换不同编辑结果

### P2

1. 更真实的检索算法
2. 更复杂的编辑动作
3. 外部模型调用封装

---

## 十一、完成标准

后端达到可交付状态时，至少应满足：

1. 任意一个 demo 输入都能识别出模式  
2. 每个 demo 输入都能返回 4 条以上参考花  
3. 生成接口能稳定返回 3 版结果  
4. 编辑接口能支持至少 2 种动作  
5. 情绪承接接口能返回 3 类卡片结果  
6. 所有接口返回结构稳定，前端可直接消费  

---

## 十二、给编程 AI 的执行建议

如果这份文档交给 code 模型开发，建议按以下顺序生成代码：

1. 搭建 FastAPI 项目骨架  
2. 定义全部 Pydantic schema  
3. 建立本地知识库 JSON 与读取逻辑  
4. 实现模式识别与语义解析 service  
5. 实现参考检索 service  
6. 实现三版结果生成 service  
7. 实现编辑 service  
8. 实现情绪承接 service  
9. 最后补齐 API route 与示例数据  

开发目标不是“做出最强模型”，而是“做出一条稳定、可演示、可被前端调用的完整链路”。
