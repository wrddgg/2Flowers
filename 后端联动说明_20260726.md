# 后端联动说明（2026-07-26）

本文档用于提醒前端同学，本轮后端已经补齐的协议与需要前端配合消费的部分。

## 1. 花束生成结果新增字段

接口：`POST /api/bouquet/generate`

### 1.1 方案解释相关
- `scene_preset`
- `style_preset`
- `explanation`
- `fit_scenes`
- `usage_goal`
- `reality_advice`

前端建议：
- 在生花方案卡片列表中直接展示 `explanation`
- 在方案详情中展示 `fit_scenes / usage_goal / reality_advice`

### 1.2 花材交互相关
- `planned_flowers`
- `recognized_flowers`
- `flowers`

当前约定：
- `flowers` 默认就是前端应直接使用的交互锚点列表
- `recognized_flowers` 为同一份识别后锚点结果，便于后续调试和显式区分
- `planned_flowers` 为生成前的花材结构计划，用于兜底和后续复刻/教程联动

`flowers[]` 重点字段：
- `flower_id`
- `name`
- `category`：`main | transition | accent | linear`
- `category_label`
- `point`
- `confidence`
- `placement_zone`
- `label_side`
- `detection_origin`：`recognized | planned_fallback`
- `visible_reason`

前端建议：
- 花图修改入口优先消费 `flowers`
- 点击锚点后，展示花名与类别即可，不需要展示过多技术字段
- 若 `detection_origin = planned_fallback`，前端可弱化“已识别”语气，改成“建议替换花材”

## 2. 复刻流程第二阶段接口

接口：`POST /api/emotion/remake-preview`

用途：
- 用户先在第一阶段选择文字方案
- 再调用此接口生成现实复刻预览图与复刻计划

返回重点：
- `preview_image_url`
- `preview_status`
- `budget_level`
- `generation_brief`
- `plan`

`plan` 内重点字段：
- `title`
- `seasonality_note`
- `preserve_points`
- `selected_flowers`
- `substitute_flowers`
- `materials_note`

前端建议：
- 第一阶段只展示文字候选，不要提前展示图片
- 用户点击某个候选后，再调用本接口
- 第二阶段结果页要把预算、替花说明、现实可用性一起展示出来

## 3. 教程链路补强字段

接口：
- `POST /api/generate-tutorial`
- 查询教程状态相关接口

新增字段：
- `image_review_score`
- `image_review_issues`
- `image_retry_count`
- `image_fallback_note`
- `image_status`
- `image_review`
- `image_display_ratio`
- `image_display_fit`

前端建议：
- 在图片未加载完成时显示明确 loading 态
- 当 `image_status != done` 时，前端要展示文字兜底，不要直接留空
- 使用 `image_display_fit = contain`，避免再次裁切教程图

当前状态约定：
- `done`：教程图审核通过，可直接展示图片
- `fallback`：教程图三次尝试后仍未通过审核，前端应展示 `image_fallback_note`
- `skipped`：当前步骤未生成图片，前端可直接走文字教学态
- `failed`：仅保留给真正异常情况，正常情况下教程步骤更推荐走 `fallback` 而不是直接失败

前端展示建议：
- `image_status = fallback` 时，不要把它渲染成报错弹窗，应该渲染成“本步建议说明 + 文字步骤”
- 优先展示 `image_fallback_note`，其次再把 `image_review` 作为开发期调试信息
- `image_review_issues` 更适合开发调试或灰度阶段，不建议直接大量暴露给普通用户

## 4. 分享卡结果字段

接口：`POST /api/generate-card`

重点字段：
- `compare_layout`
- `compare_panels`
- `panel_order`
- `panel_labels`
- `scene_reason`

前端建议：
- 对比图按三图模式优先展示：`输入素材 / AI 生花 / 自制复刻`
- 卡片页应展示 `scene_reason`

## 5. 这轮前端最需要协调的事项

1. 生花方案列表页接入 `explanation / fit_scenes / usage_goal / reality_advice`
2. 修改花图交互改为直接消费 `flowers[].point + name + category`
3. 当 `detection_origin = planned_fallback` 时，调整文案避免误导成“精准识别”
4. 复刻流程改为“两阶段”：先文字选项，再调 `/api/emotion/remake-preview`
5. 教程页增加加载态、失败态和 `contain` 展示策略
6. 教程页对 `image_status = fallback` 增加“文字兜底卡片”展示，不要仍按图片失败处理
7. 分享卡页按后端返回的 `compare_panels` 渲染，不要写死双图布局

## 6. 用户缓存与生花记录

本轮后端新增了“运行期用户缓存”能力。

约束：
- 这是运行期内存缓存，不落数据库
- 程序重启后会自动清空
- 目的不是长期账户体系，而是解决用户切页后返回仍能续接的问题

### 6.1 独立缓存接口

接口：
- `POST /api/user-cache/progress`
- `GET /api/user-cache/progress`
- `POST /api/user-cache/records`
- `GET /api/user-cache/records`

用途：
- `progress`：保存/读取用户当前做到哪一页、对应的 `result_id / task_id / draft`
- `records`：保存/读取用户已经确认保存过的生花记录

### 6.2 现有主流程已支持自动记进度

只要前端在这些请求里带上 `user_id`，后端就会自动更新当前进度：
- `POST /api/input/analyze`
- `POST /api/bouquet/generate`
- `POST /api/bouquet/edit`
- `POST /api/emotion/build`
- `POST /api/emotion/remake-preview`
- `POST /api/generate-tutorial`
- `POST /api/generate-card`

当前约定：
- `user_id` 目前只是运行期用户标识，不需要登录体系
- 前端可以在首次进入应用时生成一个稳定 id，并在本次运行期间持续复用

### 6.3 进度恢复字段

`GET /api/user-cache/progress` 返回重点：
- `current_page`
- `mode`
- `content_id`
- `request_id`
- `result_id`
- `result_ids`
- `tutorial_task_id`
- `card_image_url`
- `draft`

前端建议：
- 进入首页时优先查询一次 `progress`
- 若存在缓存，按 `current_page` 决定恢复到分析页、生花页、编辑页、复刻页、教程页或卡片页
- `draft` 只作为界面恢复辅助，不要把它当成最终可信业务结果

### 6.4 生花记录字段

`GET /api/user-cache/records` 返回重点：
- `record_id`
- `result_id`
- `title`
- `summary`
- `bouquet_image_url`
- `card_image_url`
- `source_context`
- `scene_reason`
- `tags`
- `scene_preset`
- `style_preset`
- `saved_at`

前端建议：
- 后续可以独立做一个“我的生花记录”页
- 优先展示 `bouquet_image_url / card_image_url / title / saved_at`
- 点击记录后可直接用 `result_id` 继续恢复到对应结果页

## 7. 备注

- 本轮后端已经把问题清单中的联动项初步补齐一版。
- 花材锚点识别在测试环境下会回退到 `planned_fallback`；正式环境下会尝试走生成后再次识别。
