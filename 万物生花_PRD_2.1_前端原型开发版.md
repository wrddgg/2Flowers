# 万物生花 2.1 — 前端原型开发版 PRD

> 项目：抖音“视觉搜索：视觉 + 语音”赛道 Hackathon 参赛产品  
> 文档版本：2.1 前端原型开发版  
> 日期：2026-07-25  
> 文档用途：供前端开发人员进行原型开发、页面搭建与交互串联  
> 说明：本文件为新增版本，原始文件 `万物生花_PRD_开发交付版.md` 与 `万物生花_PRD_2.0_结构升级版.md` 均保持不变

---

## 一、这版文档的目标

这份文档不是纯概念稿，而是面向前端原型开发的工作文档。它要解决两个问题：

1. 让开发同学明确产品到底要呈现什么体验，而不是只知道“做几个页面”
2. 让评审与 Demo 视角一致，保证前端做出来的原型能准确表达产品价值

因此，这份文档会重点写清楚：

- 产品核心命题
- 页面结构与主链路
- 每个页面该出现什么模块
- 每种交互该如何触发与反馈
- 哪些文案需要强调情绪价值
- 哪些数据可以用伪数据与预置素材模拟

---

## 二、产品定义

### 2.1 一句话定位

`万物生花` 是一个把抖音内容里的视觉氛围，转化成花艺表达与情绪承接结果的多模态产品。

### 2.2 产品主张

用户在抖音中刷到一个打动自己的画面，圈选后说一句话，AI 会先把这个感觉“生”成一组花艺表达，再允许用户继续编辑、继续追问、继续浏览相似内容，最终把这份感觉承接成一张可以收藏、分享、送出或拥有的结果。

### 2.3 核心价值

产品不是在做“识花工具”，也不是在做“单次生成器”，而是在做三件连续的事：

1. `看见感觉`
让用户觉得“这个画面真的被理解了”

2. `共创表达`
让用户可以在 AI 给出的方向上继续删、换、调，而不是被动接受结果

3. `承接情绪`
让用户看到的不只是一张图，而是一份有现实落点的表达结果

---

## 三、2.1 相比 2.0 的新增升级

### 3.1 从“结果页”升级为“内容生态链路”

2.0 已经把产品从“生成玩法”升级成了“表达产品”，2.1 继续把它接回抖音生态，让“生花”不再是内容的终点，而是新的内容入口。

新增逻辑：

- 用户生花后，可继续浏览 `同花感内容`
- 可查看 `相似氛围视频 / 图片`
- 可用当前生花结果继续做二次创作

这样产品从：

`看内容 → 生花 → 结束`

升级为：

`看内容 → 生花 → 看更多同感觉内容 → 编辑共创 → 收藏分享`

### 3.2 从“复刻链路”升级为“情绪承接链路”

前端展示中，弱化花材、教程、供应链、复刻难度这些高风险内容，改强调：

- 这份感觉适合送给谁
- 为什么适合
- 怎样保存这份感觉
- 怎样以更贴近现实的方式拥有它

因此，结果页不再以“花材替换清单”为中心，而改为以 `情绪承接卡` 为中心。

### 3.3 强化“可编辑共创”

2.1 明确把图生图能力前置到交互体验中。用户在生花结果上不再只能继续语音追问，还可以：

- 点选某一朵花删除
- 圈选一片区域做局部替换
- 用滑杆调整整体风格
- 点击“再克制一点”“更适合送礼”等快捷操作

这会显著提升原型的可玩性与技术感。

### 3.4 新增“模板模式系统”

最重要的新增点是：不同类型内容不走同一种生花逻辑。

同样是“生花”，以下三种输入在产品目标、结果结构和用户期待上都不同：

- 从场景生
- 从花再生
- 从人生生

2.1 会把这三种模式正式纳入产品结构。

---

## 四、核心命题

`万物生花` 不是把万物简单变成花，而是把万物带来的感觉，转成一份可以被花表达、被用户编辑、被现实承接的结果。

为了让这一点在前端上被明确感知，整个产品界面必须始终遵守三个原则：

1. `先让人觉得美`
第一屏先打动人，而不是先解释技术

2. `再让人觉得懂`
每一次追问、编辑、调整都要让用户明显感到结果更像自己

3. `最后给下一步`
收藏、分享、送礼、拥有，必须有一个可继续走的出口

---

## 五、生花模式系统

这是 2.1 最重要的新增结构。前端与后端都要围绕它设计。

### 5.1 模式总览

| 模式名 | 适用输入 | 用户真正想要的 | 生成重点 | 前端展示重点 |
|------|---------|--------------|---------|-------------|
| 从场景生 | 风景、空间、天气、街景、餐桌、旅行画面 | 留住一个氛围 | 抽取色调、光影、温度、留白、仪式感 | 氛围表达与同花感内容 |
| 从花再生 | 花束、花艺装置、鲜花内容、本来就是花 | 再设计、再升级、再编辑 | 保留原花感，做延展、优化、替换、重组 | 可编辑能力与版本差异 |
| 从人生生 | 人物、穿搭、关系场景、人生时刻 | 把“这个人/这个时刻”的感觉变成花 | 提取气质、关系、身份、场景分寸 | 情绪价值与送礼适配 |

### 5.2 面向用户的模式名称

为了更自然，前端可以展示为三个模式卡：

- `从场景生`
把风景、空间和画面氛围变成花

- `从花再生`
把已有花束或花艺重新生长成新的表达

- `从人生生`
把人物、关系和人生时刻变成花

这里建议把“从花生”展示为 `从花再生`，因为更自然，也更容易让用户理解“我是在已有花上继续做”。

### 5.3 每种模式的执行逻辑

#### A. 从场景生

适合内容：

- 晚霞、雨景、雪景、海边、街景
- 咖啡馆、窗边、婚礼布景、餐桌、房间角落
- 美食、静物、物件构图

核心逻辑：

- 不强调具体对象
- 强调色调、气氛、空间感、光线、构图节奏
- 结果应更偏“氛围花”

前端重点：

- 首屏结果更大图、更少字
- 结果命名更像作品标题
- 推荐更多“同花感内容”

#### B. 从花再生

适合内容：

- 已有花束
- 花艺博主内容
- 线下花店里的成品花
- 婚礼、活动中的花艺装置

核心逻辑：

- 不从零抽象，而是在原花基础上“再设计”
- 保留原本的花感
- 突出编辑和替换能力

前端重点：

- 必须有“点选删除”“局部替换”“保留主花”等操作
- 结果页应更像编辑器，而不是一次性生成结果页
- 文案强调“再做一版”“更适合你”“更适合当前场景”

#### C. 从人生生

适合内容：

- 人物穿搭
- 某个人的照片
- 毕业、升职、结婚、和解、探望等人生时刻
- 带强关系和身份信息的内容

核心逻辑：

- 优先理解气质、身份、关系距离、表达分寸
- 不是“这个人像什么花”，而是“这份人生感受适合怎样的花艺表达”

前端重点：

- 强调“送谁”“适合什么关系”“为什么合适”
- 结果卡要突出情绪承接
- 同花感内容可以弱一些，情绪承接卡更重一些

### 5.4 模式选择策略

前端和后端共同使用“自动识别 + 用户可切换”的双保险策略。

#### 前端策略

默认流程：

1. 用户进入生花页
2. 系统根据当前输入内容，预选一个模式
3. 页面顶部展示模式卡与一句解释
4. 用户可手动切换模式

例如：

- 当前检测为 `从场景生`
- 顶部提示：`检测到这是一个场景氛围画面，已为你进入“从场景生”模式`
- 下方保留 `切换到 从花再生 / 从人生生`

#### 后端策略

后端输出一个模式判断结构：

```json
{
  "detected_mode": "scene",
  "confidence": 0.82,
  "evidence": ["landscape", "sunset", "wide_view", "low_human_focus"]
}
```

如果置信度高，前端默认进入该模式；如果置信度低，前端展示模式选择弹层，让用户确认。

### 5.5 模式判断规则

可执行规则建议如下：

| 判断维度 | 场景生 | 花再生 | 人生生 |
|--------|------|-------|-------|
| 主体识别 | 场景/空间占主导 | 花束/花朵占主导 | 人像/人物关系占主导 |
| 语音关键词 | 晚霞、雨、房间、桌面、氛围 | 花束、配色、删掉、换花、包装 | 她、他、朋友、生日、升职、适合送 |
| 输出目标 | 氛围表达 | 编辑重构 | 情绪适配 |
| 前端主按钮 | 看同花感内容 | 继续编辑 | 生成送礼卡 |

备注：后端内部 `mode` 字段建议分别使用 `scene / flower / life`，前端展示统一用 `从场景生 / 从花再生 / 从人生生`。

### 5.6 模式系统的价值

这套系统的价值很高，因为它解决了一个根本问题：用户虽然都在“生花”，但期待根本不同。

- 看晚霞的人，更想留住感觉
- 看现成花束的人，更想编辑与升级
- 看人物或人生时刻的人，更想表达关系和情绪

如果三种情况用同一套前端逻辑，用户会感觉“结果不够懂我”。模式系统能明显提升产品的理解力。

---

## 六、产品主链路

### 6.1 核心链路

```text
内容触发 / 实景触发
    ↓
圈选 + 语音输入
    ↓
模式识别 / 用户确认模式
    ↓
AI 生花
    ↓
三版结果 + 一句话解释
    ↓
编辑共创
删除 / 替换 / 局部重绘 / 风格滑杆 / 语音微调
    ↓
内容延展
查看同花感内容 / 相似情绪内容
    ↓
情绪承接
收藏卡 / 送礼卡 / 拥有卡
    ↓
分享 / 保存 / 继续追问
```

### 6.2 产品中的三个关键页面时刻

| 时刻 | 页面目标 | 用户感受 |
|------|---------|---------|
| 模式确认 | 让用户知道“我现在是在用什么生花” | 这个产品知道我在做什么 |
| 结果与编辑 | 让用户看到结果可继续被塑造 | 不是一次性结果，而是可以共创 |
| 情绪承接 | 让用户看到结果有现实出口 | 这份感觉可以被留下、被送出、被拥有 |

---

## 七、信息架构

### 7.1 页面结构

建议前端原型至少包含以下 6 个页面或状态页：

1. `首页 / 入口页`
2. `内容选择 / 圈选页`
3. `模式确认页`
4. `生花结果页`
5. `编辑共创页`
6. `情绪承接页`

如果时间紧，也可以将 `模式确认页` 合并到 `生花结果页` 顶部，将 `编辑共创页` 做成结果页中的切换视图。

### 7.2 推荐导航结构

| 页面 | 主任务 | 是否必须单独成页 |
|------|-------|----------------|
| 首页 | 展示入口、推荐示例、模式入口 | 是 |
| 内容选择页 | 视频/图片选择、圈选 | 是 |
| 模式确认页 | 确认“从场景生 / 从花再生 / 从人生生” | 可合并 |
| 生花结果页 | 展示 3 版结果 | 是 |
| 编辑共创页 | 局部编辑、快捷调整、语音微调 | 是 |
| 情绪承接页 | 收藏卡、送礼卡、拥有卡、相似内容流 | 是 |

---

## 八、页面详细设计

以下内容为前端原型开发的核心部分。

### 8.1 首页 / 入口页

#### 页面目标

让用户一眼理解产品做什么，并知道可以从不同内容类型开始“生花”。

#### 页面模块

1. `主标题区`
- 标题：`把看到的感觉，生成一束花`
- 副标题：`从场景生、从花再生、从人生生`

2. `模式入口卡`
- 三张并列卡片
- 每张卡片包含：名称、说明、示例缩略图

3. `推荐示例区`
- 预置 6-9 张 Demo 素材卡
- 每张卡片显示标签：`晚霞` `穿搭` `花束` `婚礼` `雨景` `生日`

4. `最近生花区`
- 若做时间允许，可加一个轻量历史记录区

#### 交互说明

- 点击任一模式卡进入对应模式
- 点击示例素材直接进入圈选页
- 模式卡需要有明确选中态

#### 设计重点

首页一定要让用户知道：这不是只有一种玩法，而是 3 种不同的“生花”方式。

### 8.2 内容选择 / 圈选页

#### 页面目标

让用户选择要处理的内容，并明确圈选对象。

#### 页面模块

1. `内容画布`
- 展示视频封面帧或图片
- 支持框选或点选主体区域

2. `顶部模式提示`
- 显示当前模式
- 支持切换模式

3. `语音输入区`
- 麦克风按钮
- 语音转文本展示区域

4. `快捷提示词`
- 场景生：`把这场雨变成花`
- 花再生：`把这束花做得更克制`
- 人生生：`把她今天的感觉变成花`

#### 状态要求

- 默认态
- 已圈选态
- 录音中
- 语音转写中
- 可提交态

#### 设计重点

这里不要做得像复杂编辑器，要像一个“轻量而自然的内容触发器”。

### 8.3 模式确认页

#### 页面目标

让用户知道系统判断他当前在用哪种生花逻辑，并允许纠正。

#### 页面模块

1. `模式判断卡`
- 标题：`已为你匹配到“从场景生”`
- 说明：`因为当前内容以空间氛围与整体色调为主`

2. `切换模式区`
- 三个模式卡并列
- 当前模式高亮

3. `继续按钮`
- 文案：`开始生花`

#### 设计重点

如果时间不足，可以把这一块做成结果页顶部的条状提示，而不是单独成页。

### 8.4 生花结果页

#### 页面目标

把生花结果第一次完整地呈现出来，让用户感到惊艳，同时愿意继续编辑。

#### 页面模块

1. `顶部模式条`
- 当前模式
- 当前输入描述
- 返回切换模式入口

2. `三版结果区`
- 三张结果卡横排或轮播
- 每张卡包含：
  - 花艺图
  - 结果标题
  - 一句话解释
  - 适用标签

3. `快捷操作区`
- `选这一版`
- `再克制一点`
- `更适合送人`
- `看相似内容`
- `继续编辑`

4. `同花感内容流入口`
- 横向卡片流
- 展示 4-6 条伪内容

#### 页面状态

- 生成中
- 结果已出
- 切换版本中
- 加载相似内容中

#### 设计重点

这里是视觉高潮页。  
UI 应更偏“作品展示”，少放技术说明。

### 8.5 编辑共创页

#### 页面目标

证明产品不是一次性生图，而是可控、可继续共创的。

#### 页面模块

1. `大图编辑区`
- 当前选定花艺图
- 支持点击花朵或区域

2. `编辑工具栏`
- 删除
- 替换
- 重绘
- 保留主花
- 复原

3. `整体风格滑杆`
- 热烈 ↔ 克制
- 丰盈 ↔ 留白
- 自然 ↔ 正式
- 治愈 ↔ 浪漫

4. `语音微调区`
- 支持输入：`把边缘再松一点`
- 支持输入：`中间不要这么艳`

5. `快捷建议区`
- `更适合送朋友`
- `预算友好一点`
- `把白花换多一点`

#### 交互动作

| 动作 | 前端表现 | 反馈 |
|------|---------|------|
| 点选一朵花 | 高亮选中 | 底部弹出操作条 |
| 点击删除 | 触发局部重绘加载态 | 显示新结果 |
| 圈选一块区域 | 显示遮罩框 | 弹出“替换 / 重绘 / 调浅” |
| 调整滑杆 | 预览变化趋势 | 松手后更新结果 |
| 语音微调 | 转写 + 等待 | 显示新结果与一句解释 |

#### 设计重点

这个页面是产品“技术感”的核心。  
前端哪怕用伪动效和预置结果切换，也必须把“我可以编辑”表达清楚。

### 8.6 情绪承接页

#### 页面目标

把结果从“好看”推进到“有下一步”。

#### 页面模块

1. `结果主卡`
- 当前最终花艺图
- 标题
- 一句话情绪说明

2. `三类承接卡`

`收藏卡`
- 保存图
- 一句话标题
- 适合发抖音文案

`送礼卡`
- 适合送给谁
- 为什么合适
- 避免什么误解

`拥有卡`
- 同感觉现货版
- 预算友好版
- 轻量桌花版

3. `继续看同花感内容`
- 相关视频 / 图片卡片流

4. `分享按钮区`
- 保存到相册
- 生成分享卡
- 发布到抖音

#### 设计重点

这里必须弱化“技术”和“花材”，强调“情绪被承接”。

---

## 九、前端组件清单

为了便于开发，建议前端按组件化思路拆分。

### 9.1 核心组件

| 组件名 | 用途 |
|------|------|
| `ModeCard` | 展示三种生花模式 |
| `SampleContentCard` | 展示示例内容素材 |
| `SelectionCanvas` | 图片/视频圈选区域 |
| `VoiceInputBar` | 语音录入与转写展示 |
| `BloomResultCard` | 三版生花结果卡 |
| `SimilarContentRail` | 横向相似内容流 |
| `EditToolbar` | 删除、替换、重绘、复原 |
| `StyleSliderGroup` | 风格滑杆组 |
| `EmotionCarryCard` | 收藏卡 / 送礼卡 / 拥有卡 |
| `ActionFooter` | 页面底部主按钮区 |

### 9.2 建议状态

每个核心组件至少要有以下状态：

- 默认态
- Hover / 选中态
- 加载态
- 结果切换态
- 空状态

特别说明：

- `BloomResultCard` 必须有选中态
- `SelectionCanvas` 必须有已框选态
- `EmotionCarryCard` 必须有可点击展开态

---

## 十、交互细节要求

### 10.1 模式切换

- 模式切换不应隐藏在设置里，必须前置可见
- 模式切换后，结果卡的说明文案也要同步变化
- 如果用户从 `从场景生` 切到 `从人生生`，前端要出现一句过渡提示：
  - `已改为按人物与关系来理解这张内容`

### 10.2 三版结果命名

不要继续使用太模板化的：

- 浪漫版
- 简约版
- 野趣版

建议采用“标题 + 小标签”的方式：

- `暮色留白`
  - 标签：克制 / 温柔 / 适合收藏
- `风里热烈`
  - 标签：丰盈 / 适合送礼
- `雨后安静`
  - 标签：治愈 / 自然

### 10.3 可编辑能力的呈现方式

前端不需要真实做复杂图像编辑，但必须在交互上呈现这些动作：

- 点击花朵有选中态
- 删除后有局部刷新动效
- 替换后图面发生明显变化
- 滑杆拖动时有预览反馈

### 10.4 情绪承接文案

前端文案应避免过于技术化的表达。

少用：

- 花材替换
- 相似度 86%
- 翻译层
- 供给匹配

多用：

- 这份感觉适合送给谁
- 这版更克制，也更不容易误解
- 保留了你喜欢的暖光和留白
- 更适合今天就送出

---

## 十一、视觉与文案规范

这一部分直接服务前端原型呈现，避免页面做成普通工具页。

### 11.1 视觉基调

产品整体不应太像后台工具，也不应太像电商详情页，而应更接近“内容创作工具 + 情绪表达作品页”。

建议基调：

- 主色：偏暖白、浅灰、低饱和浅紫或浅粉作为点缀
- 大图优先：结果页和编辑页必须让花艺图成为视觉中心
- 卡片分层清晰：模式卡、结果卡、承接卡要明显区分
- 文字不过密：尽量让用户先看图，再读一句解释

### 11.2 页面气质

| 页面 | 气质要求 |
|------|---------|
| 首页 | 像内容入口，要轻、要吸引点击 |
| 生花结果页 | 像作品展示页，要美、要有停留感 |
| 编辑页 | 像轻编辑器，要清楚、可操作 |
| 情绪承接页 | 像送礼建议页，要温和、可信 |

### 11.3 按钮文案建议

主按钮建议使用更自然的表达，而不是技术按钮名。

少用：

- 提交
- 生成
- 重新计算
- 应用修改

多用：

- 开始生花
- 看这一版
- 再改一点
- 更适合送人
- 留住这种感觉

### 11.4 页面中应避免的表达

前端展示时，以下词汇尽量不作为主文案：

- 算法
- 相似度计算
- 花材替代
- 供给匹配
- 模型推理

这些词最多出现在次级说明或调试信息中，不能占据页面中心。

### 11.5 页面中应强化的表达

前端核心文案要反复强化以下感觉：

- 这个画面被理解了
- 这个结果可以继续改
- 这份感觉有不同表达方式
- 这不是一张图，而是一份可以被带走的结果

---

## 十二、伪数据与素材方案

为了让前端原型高质量落地，建议直接使用预置素材，而不是现场动态生成全部结果。

### 11.1 素材池分类

| 素材池 | 数量建议 | 用途 |
|------|---------|------|
| 场景素材池 | 8-12 张 | 晚霞、雨景、婚礼、咖啡馆、餐桌、房间角落 |
| 花束素材池 | 6-9 张 | 已有花束、花店成品、婚礼花艺 |
| 人物素材池 | 6-9 张 | 穿搭、人像、关系场景、生日/毕业等 |
| 生花结果池 | 每类 3 版以上 | 用于结果页切换 |
| 相似内容池 | 每类 6 条以上 | 用于“同花感内容流”展示 |

### 11.2 推荐 Demo 画像

前端原型至少预置 3 个示例用户路径：

1. `场景型`
晚霞 → 从场景生 → 看更多同花感内容 → 收藏卡

2. `编辑型`
现成花束 → 从花再生 → 点选删除 → 局部替换 → 更适合送礼

3. `关系型`
人物穿搭 / 人生时刻 → 从人生生 → 生成送礼卡 → 分享

### 11.3 伪数据结构建议

```json
{
  "content_id": "scene_001",
  "content_type": "scene",
  "title": "海边晚霞",
  "mode": "scene",
  "voice_input": "把这种感觉变成花",
  "results": [
    {
      "id": "r1",
      "name": "暮色留白",
      "tags": ["克制", "温柔", "适合收藏"],
      "image": "/mock/scene_001_r1.png",
      "summary": "保留了晚霞的暖光与安静留白"
    }
  ],
  "similar_contents": [
    {
      "id": "sim_001",
      "title": "窗边夕阳",
      "cover": "/mock/sim_001.png",
      "reason": "同样的暖橙光线与静谧感"
    }
  ]
}
```

---

## 十三、前后端协同建议

虽然当前重点是前端原型，但为了避免后续联调成本，建议前端从一开始就按“可被真实接口替换”的思路做。

### 12.1 前端需要的关键字段

| 字段 | 用途 |
|------|------|
| `detected_mode` | 当前模式展示 |
| `mode_confidence` | 是否需要用户确认模式 |
| `voice_input_text` | 用户输入展示 |
| `result_list` | 三版生花结果 |
| `editable_actions` | 当前支持的编辑动作 |
| `similar_contents` | 同花感内容流 |
| `emotion_cards` | 收藏卡 / 送礼卡 / 拥有卡 |

### 12.2 推荐接口拆分

建议逻辑上拆成这几个接口，即使前端先用 mock 数据：

- `内容识别与模式判断`
- `生花结果生成`
- `局部编辑更新`
- `相似内容推荐`
- `情绪承接结果生成`

这样后续替换真实接口时改动最小。

---

## 十四、MVP 范围

### 13.1 MVP 必须完成

前端原型至少需要把以下 5 件事做清楚：

1. 用户能看懂有三种不同模式  
2. 用户能完成一次内容圈选与语音输入  
3. 用户能看到三版有明显差异的生花结果  
4. 用户能进行至少一种局部编辑  
5. 用户能看到一个情绪承接结果页

### 13.2 MVP 可以不做真逻辑，但要做真体验

以下能力可以用预置结果模拟：

- 后端模式识别
- 图像局部重绘
- 相似内容推荐
- 送礼适配解释

但前端必须把体验做得像真的，因为评委看到的是“体验完整度”，不是接口真假。

### 13.3 不建议在 MVP 里重讲的内容

以下内容在原型中不宜做太重：

- 花材包分级体系
- 复杂花材清单
- 供应链过程
- 冷链与物流
- 过细的价格计算

这些内容只会稀释产品的诗意和智能感。

---

## 十五、答辩时前端页面要帮产品说的话

如果前端原型做得对，即使不靠很多口头解释，页面本身也应该能传达以下信息：

### 14.1 首页要传达

`这不是一个单一的生图工具，而是一套不同语义下的生花方式。`

### 14.2 结果页要传达

`AI 真的理解了这个画面的感觉，而且给出了多个可选表达。`

### 14.3 编辑页要传达

`这个结果不是固定的，你可以继续把它调成更像自己的样子。`

### 14.4 承接页要传达

`这份感觉不是停留在屏幕里，而是能继续被保存、分享、送出或拥有。`

---

## 十六、最终建议

对前端开发来说，这份产品最应该被做出来的，不是“技术流程图”，而是四种感受：

1. `被内容击中`
2. `被 AI 理解`
3. `可以继续共创`
4. `情绪被接住了`

如果这四件事在原型里成立，`万物生花` 就不再只是一个花艺创意，而会成为一个更像抖音生态产品的完整方案。

2.1 的最终结构可以概括为一句话：

**用模式系统保证“懂你在生什么”，用编辑能力保证“你能继续塑造它”，用情绪承接保证“这份感觉有现实出口”。**
