# 万物生花 2.3 — 正式产品文档

> 项目：抖音“视觉搜索：视觉 + 语音”赛道 Hackathon  
> 版本：2.3 正式版  
> 用途：产品统一方案、研发对齐、答辩依据  
> 说明：本文件为新增正式版，历史文档保持不变

---

## 一、产品定义

`万物生花` 是一个把内容中的视觉感觉转成花艺表达，再把花艺表达承接为收藏、送礼、分享与拥有结果的多模态产品。

用户在抖音内容或真实场景中圈选一个画面并说一句话，系统先理解输入内容的语义与氛围，再到抖音生态中检索相似花内容作为真实参考，融合生成结果花束。生成后，用户可以继续点选、删除、替换、语音微调，最终产出一张有明确情绪价值的成品花束卡片。

---

## 二、核心价值

产品价值集中在三件事：

1. `理解感觉`
不是识别画面里“有什么”，而是理解画面在表达什么氛围、情绪和关系感。

2. `参考真实`
不是让模型凭空想象一束花，而是从真实内容中的花束、花艺装置和相关图片中提取参考，让结果更可信。

3. `允许共创`
生成不是终点，用户可以继续删、换、调，让结果更像自己想要的表达。

---

## 三、三种生花模式

不同内容类型不采用同一套生花逻辑。系统支持三种模式。

| 模式 | 输入内容 | 核心目标 | 输出重点 |
|------|---------|---------|---------|
| 从场景生 | 晚霞、雨景、空间、静物、街景、餐桌 | 留住一个氛围 | 生成“氛围花”，强调色调、光影、空间感 |
| 从花再生 | 花束、花艺装置、花店成品、鲜花图片 | 在原花基础上再设计 | 强调局部编辑、替换、优化、重组 |
| 从人生生 | 人物、穿搭、关系场景、人生时刻 | 把某个人或某个时刻变成花 | 强调送礼适配、分寸、关系与情绪表达 |

模式策略采用 `自动识别 + 用户可切换`：

- 系统根据输入内容预判模式
- 用户可以手动切换模式
- 不同模式会影响检索逻辑、参考内容类型和结果解释方式

---

## 四、主链路

```text
内容输入
→ 圈选区域 + 语音输入
→ 模式识别
→ 语义理解
→ 检索真实参考花内容
→ 用户选择参考花或跳过参考
→ 融合生成 3 版结果
→ 选择一版进入共创编辑
→ 点选花朵查看信息 / 删除 / 替换 / 语音微调
→ 生成最终花束卡片
→ 收藏 / 分享 / 送礼 / 拥有
```

---

## 五、核心能力

### 5.1 语义理解层

系统对输入内容进行视觉与语音联合理解，提取以下结构化信息：

- 内容主体类型
- 主色调与饱和度
- 光线、空间感、留白感
- 情绪标签
- 关系与使用意图
- 模式候选

这一层负责回答：`这张内容表达的感觉是什么？`

### 5.2 真实参考增强层

这是 2.3 的核心升级能力。

系统基于语义理解结果，在抖音生态或 Demo 使用的本地花视频知识库中检索具有相似特征的花内容，作为生成参考。参考内容可以来自：

- 真实视频中的花束
- 真实图片中的花艺装置
- 场景相关的送礼花内容
- 风格相近的花店成品图

检索维度包括：

- 视觉相似：颜色、密度、构图、体量
- 情绪相似：温柔、热烈、克制、治愈、梦幻
- 场景相似：生日、探望、升职、日常、自用
- 表达相似：适合收藏、适合送礼、适合桌花、适合纪念

这一层负责回答：`真实世界里，有哪些花能承接这种感觉？`

### 5.3 参考花选择器

系统检索出参考花内容后，用户可以主动选择参考策略。

支持三种参考强度：

| 策略 | 含义 |
|------|------|
| 不参考 | 只基于输入内容语义生成 |
| 轻参考 | 借用参考花的颜色、局部风格或结构特征 |
| 强参考 | 更明确地吸收参考花的花种组合、结构与包装方式 |

用户也可以直接选择某一条参考花内容，或者明确选择“不需要参考这些视频里的花”。

### 5.4 融合生成层

融合生成层将两部分信息结合：

1. 用户输入内容的语义与氛围
2. 真实参考花的视觉与场景特征

最终生成 3 版明显不同的候选花束，每版都包含：

- 花束主视觉图
- 标题
- 一句话解释
- 参考来源说明

这里的生成目标不是“绝对真实复刻”，而是“在保持原始感觉的同时，更接近真实世界里可能出现的花”。

### 5.5 共创花束页

生成完成后，用户进入共创花束页，而不是停留在静态结果页。

共创花束页支持两类能力：

#### A. 花朵解释

用户点击花束中的某一朵花，弹出信息卡片，展示：

- 花名
- 类型
- 简短寓意
- 它在这束花中的作用
- 为什么它会出现在这里

#### B. 花朵编辑

用户可以继续调整结果，包括：

- 点击删除某一朵花
- 圈选一个区域进行替换或重绘
- 用语音指令调整花束

示例语音：

- `删掉中间这朵粉花`
- `把白花再多一点`
- `包装不要这么隆重`
- `更适合送刚升职的朋友`

### 5.6 情绪承接层

最终结果不只是一张图，而是一份有出口的情绪结果。系统为用户提供三类承接结果：

| 卡片 | 作用 |
|------|------|
| 收藏卡 | 保存图、标题、适合发布的短文案 |
| 送礼卡 | 适合送给谁、为什么合适、应避免什么 |
| 拥有卡 | 同感觉现货版、预算友好版、轻量桌花版 |

---

## 六、主要页面

产品原型或 Demo 至少包含以下页面或视图：

1. 首页
2. 内容选择页
3. 模式确认页
4. 生花结果页
5. 参考花选择器
6. 共创花束页
7. 情绪承接页

---

## 七、MVP 范围

Hackathon MVP 需要证明 6 件事：

1. 用户能输入图片并圈选内容  
2. 系统能识别模式并生成 3 版结果  
3. 系统能检索出一组真实参考花内容  
4. 用户能选择参考策略或跳过参考  
5. 用户能在共创页点击花朵并进行至少一种编辑  
6. 用户能得到收藏卡、送礼卡或拥有卡中的至少一种结果  

MVP 中允许使用本地知识库、预置素材和伪数据，不要求接入真实抖音数据。

---

## 八、正式方案中的关键判断

这版最终方案不再把重点放在“DIY 复刻”或“花材供应链”上，而明确聚焦在：

- 理解内容中的感觉
- 用真实参考增强结果可信度
- 让用户与 AI 共创一束花
- 把结果承接为明确的情绪价值

一句话概括这版正式方案：

**万物生花，不是让 AI 凭空生成花，而是在理解你的内容之后，参考真实世界里的花，和你一起完成一束更真实、更适合表达的花。**
